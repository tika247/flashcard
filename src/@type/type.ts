interface GlobalPropsType {
  $modalMode: {
    type: string | boolean;
    index?: number;
  };
}
interface WordType {
  word: string;
  meaning: string;
  japanese: string;
  example: string;
  note: string;
}
